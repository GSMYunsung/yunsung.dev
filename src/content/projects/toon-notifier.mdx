---
title: "Toonify"
why: "내가 좋아하는 인스타툰 새 화를 자꾸 놓치기 싫어서 만들었습니다"
description: "인스타그램 인스타툰의 새 에피소드를 자동 감지해 푸시 알림으로 알려주는 React Native 앱. GitHub Actions 배치로 3시간마다 업데이트를 체크합니다."
techStack: ["React Native", "Expo", "Supabase", "GitHub Actions"]
github: "https://github.com/GSMYunsung/Toonify"
demo: "https://apps.apple.com/kr/app/toonify/id6794237802"
period: "2025"
category: "사이드"
role: "기획 · 설계 · 개발 (1인)"
tools:
  [
    "React Native",
    "Expo SDK 54",
    "Supabase",
    "GitHub Actions",
    "AsyncStorage",
    "hasdata API",
    "OCR.space API",
    "Expo Push API",
  ]
featured: false
order: 3
---

## 만든 이유

인스타그램 팔로우 알림은 팔로우한 모든 계정의 게시물이 섞여 오기 때문에, 좋아하는 인스타툰 새 화만 골라서 받는 방법이 없었습니다. 여러 작가를 팔로우하면 업데이트를 놓치기 쉽고, 결국 구독하던 시리즈를 잊어버리게 됩니다. 관심 작가를 등록해두면 새 에피소드가 올라올 때 알림을 받을 수 있는 앱을 만들었습니다.

## 주요 기능

- **인스타툰 구독 관리** — 마지막으로 읽은 화의 인스타 링크를 붙여넣으면 계정명·시리즈명·화수를 자동 파싱. 스와이프 삭제·수정 지원
- **자동 에피소드 감지** — 최신 게시물 12개를 최신순 스캔, 캡션 키워드 매칭으로 새 화 감지. 캡션에 화수가 없으면 OCR로 이미지에서 직접 인식
- **복수 화수 동시 알림** — "3화, 4화가 추가되었어요" 형식으로 한 번에 알림
- **에피소드 목록** — 화수 탭 → 해당 인스타 게시물로 이동 + 읽음 자동 처리
- **완결 자동 처리** — `완` / `완결` 패턴 감지 또는 3주 이상 새 포스트 없으면 완결 처리
- **푸시 알림** — 새 에피소드 발견 시 Expo Push API로 즉시 전송
- **라이트 / 다크 모드** — 시스템 기본값 연동 및 수동 전환

## 배치 서버 없이 3시간 주기 감지

**배경** 3시간마다 인스타그램을 확인하고 푸시 알림을 보내는 구조가 필요했습니다. 별도 서버를 운영하는 건 유지비와 관리 부담이 컸습니다.

**행동 & 결과** GitHub Actions를 3시간 인터벌 배치 서버로 활용했습니다. 스크립트가 Supabase에서 구독 목록과 push token을 읽어 인스타그램 최신 게시물을 확인하고, 새 에피소드가 감지되면 Expo Push API로 알림을 전송합니다. 별도 서버 없이 운영 비용 없이 동작합니다.

## 캡션 + OCR 2단계 감지 엔진

**배경** 인스타툰 작가마다 화수 표기 방식이 달랐습니다. 캡션에 `n화` 형식으로 명시하는 경우도 있지만, 캡션에 화수가 없고 이미지 안에만 화수가 적힌 경우도 많았습니다. 캡션 매칭만으로는 감지 실패가 잦았습니다.

**행동 & 결과** 캡션에서 `n화` / `n편` / `ep.n` / `#n` 패턴으로 화수를 우선 추출하고, 캡션에 화수가 없으면 OCR.space API로 게시물 이미지에서 직접 인식하는 2단계 폴백 구조를 설계했습니다. 앱과 GitHub Actions 서버 모두 동일한 `matchingUtils.js`를 공유해 감지 로직의 단일 진실 공급원을 유지했습니다.

## last_post_id 기준점으로 OCR 호출 93% 절감

**배경** 초기 구조는 GitHub Actions가 실행될 때마다 최신 포스트 12개 전체를 처음부터 재검사했습니다. 새 포스트가 없어도 매 사이클마다 OCR을 최대 12회씩 소모했고, 툰 5개 기준 하루 약 160회의 OCR 호출이 발생했습니다.

**행동 & 결과** Supabase `toons` 테이블에 `last_post_id` 컬럼을 추가해 마지막으로 처리한 포스트 ID를 기억하고, 그 이후 포스트만 처리하도록 변경했습니다. 동일 조건에서 하루 OCR 호출이 약 12회로 줄어 **93% 절감**됐습니다. 첫 등록 시 기준점을 가장 오래된 포스트로 설정해, 이미 나와 있던 이전 화를 영원히 놓치는 버그도 방지했습니다.

## 앱·서버 매칭 로직 단일 소스 통일

**배경** 앱(`check-service.js`)에서 매칭 버그를 수정했는데, 서버(`check-toons.js`)는 수정 전 로직으로 계속 동작했습니다. 앱에서 3편만 감지할 때 서버는 5편을 감지하는 불일치가 생겼습니다.

**행동 & 결과** 공통 매칭 로직을 `matchingUtils.js`로 추출해 앱과 서버 모두 이 파일을 참조하도록 통일했습니다. 이후부터는 한 곳만 수정하면 양쪽에 동시 반영됩니다.

## 트러블슈팅

### OCR 무료 한도 소진

OCR.space API 크레딧이 빠르게 소진됐습니다.

동일한 포스트를 매 사이클마다 다시 OCR하는 구조가 원인이었습니다. `post.id`를 키로 AsyncStorage에 OCR 결과를 캐싱해, 한 번 인식한 포스트는 다시 API를 호출하지 않도록 처리했습니다. `last_post_id` 기준점 설계와 함께 적용해 OCR 호출을 **93% 절감**했습니다.

### ML Kit 온디바이스 OCR 도입 → 인식률 한계로 철회

OCR API 한도 문제를 근본 해결하기 위해 ML Kit 온디바이스 OCR을 도입했습니다.

일반 폰트에서는 85% 이상이었지만 인스타툰 썸네일의 아트체·손글씨 폰트에서 30~40% 수준으로 떨어졌습니다. 이미지 전처리, 신뢰도 임계값 조정 등을 시도했지만 개선 폭이 10% 내외에 그쳤습니다. OCR 호출 없는 장점보다 오감지 비용이 더 컸기 때문에 ocr.space API로 되돌아가고, 대신 캐싱으로 호출 자체를 줄이는 방향으로 해결했습니다.

### 매칭 로직 오탐 4건 연속 수정

OCR 엔진 교체 이후 오탐이 연속으로 발생했습니다. 증상은 달랐지만 원인은 하나였습니다 — 조건이 너무 느슨했습니다.

- `keyWords=[]`일 때 `minMatch=0`이 되어 모든 캡션이 매칭됨
- 캡션은 `minMatch=2` 기준인데 OCR은 1개만 있어도 통과 — OCR이 우회 경로가 됨
- 완결 감지 시 키워드 1개만 있어도 통과하는 분기가 오탐 경로로 작동

모두 `keyWords.length > 0` 가드 추가 + `minMatch` 기준 일원화로 해결했습니다.

### 개발 빌드 Metro 서버 localhost 문제

EAS 개발 빌드에서 Metro 서버가 `localhost:8081`로 고정되어 폰에서 접근이 불가했습니다.

프로젝트 루트에 `ios`/`android` 폴더가 존재하면 Expo CLI가 로컬 네이티브 빌드 환경으로 인식해 localhost를 사용하는 것이 원인이었습니다. 두 폴더를 삭제하니 올바른 LAN IP로 URL이 생성됐습니다.
